## Dotfiles for configuring VIM and terminal aliases

### Install

Use this:

```sh
sudo curl -L https://github.com/DougBR/ws_dotfiles/blob/master/install.sh -o ~/install_ws_dotfiles && sudo chmod +x ~/install_ws_dotfiles && ~/install_ws_dotfiles -i
```
